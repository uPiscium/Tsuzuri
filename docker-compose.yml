services:
  tsuzuri-api:
    build: .
    image: tsuzuri:local
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./outputs:/app/outputs
    restart: unless-stopped
